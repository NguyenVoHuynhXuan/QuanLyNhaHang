<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xuan.btandroid">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".giaodien.dangnhap_main"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".giaodien.dangki">

        </activity>
        <activity android:name=".giaodien.trangchu"
            android:label="OrderFood"
            >
            <intent-filter>
                <action android:name="android.intent.action.VIEW"></action>
                <action android:name="android.intent.action.GET_CONTENT"></action>

            </intent-filter>

        </activity>
        <activity android:name=".DialogActivity.thembanan"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:label="Thêm bàn ăn"
            >

        </activity>
        <activity android:name=".DialogActivity.themloaimonan"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:label="Thực đơn"
            >
        </activity>
    </application>

</manifest>